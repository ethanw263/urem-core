Layer 23 Decision — Recognition Cost as System-Induced Geometry:
In transport-based RDE, the cost function c
R
	​

(x,y) should represent the difficulty of reallocating recognition between objects within a recognition system. It may depend on geography, networks, feature similarity, audience overlap, institutional pathways, or information visibility, but only where those mechanisms are theoretically justified. The cost function induces a recognition geometry (Ω,c
R
	​

), which may differ from ordinary geographic distance.